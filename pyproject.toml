[project]
name = "agency-availability-monitor"
version = "0.1.0"
description = "Read-only HTTPS, SLO, and incident monitoring"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
