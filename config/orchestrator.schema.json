{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://superset-agent-orchestrator.dev/schemas/orchestrator.schema.json",
  "title": "Superset Agent Orchestrator configuration",
  "description": "Machine-independent local discovery and child-process policy.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "superset": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "executable": {
          "type": "string",
          "minLength": 1,
          "description": "Absolute path or executable name. Relative paths containing separators are rejected at runtime."
        },
        "hostState": {
          "type": "string",
          "minLength": 1,
          "description": "Absolute path to Superset host state. Relative paths are rejected at runtime."
        }
      }
    },
    "timeouts": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "discoveryMs": { "$ref": "#/$defs/timeout" },
        "startupMs": { "$ref": "#/$defs/timeout" },
        "shutdownMs": { "$ref": "#/$defs/timeout" }
      }
    },
    "environment": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "allow": {
          "type": "array",
          "items": { "$ref": "#/$defs/environmentName" },
          "uniqueItems": true,
          "default": []
        },
        "set": {
          "type": "object",
          "propertyNames": { "$ref": "#/$defs/environmentName" },
          "additionalProperties": { "type": "string" },
          "default": {}
        }
      }
    },
    "concurrency": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "global": { "$ref": "#/$defs/positiveLimit" },
        "perHost": { "$ref": "#/$defs/positiveLimit" },
        "perProject": { "$ref": "#/$defs/positiveLimit" },
        "perAgent": { "$ref": "#/$defs/positiveLimit" },
        "perWorkspace": { "$ref": "#/$defs/positiveLimit" },
        "maxQueued": {
          "type": "integer",
          "minimum": 0,
          "maximum": 10000
        },
        "pressureHookTimeoutMs": {
          "type": "integer",
          "minimum": 1,
          "maximum": 2147483647
        },
        "overload": {
          "type": "string",
          "enum": ["queue", "reject"]
        }
      }
    }
  },
  "$defs": {
    "positiveLimit": {
      "type": "integer",
      "minimum": 1,
      "maximum": 10000
    },
    "timeout": {
      "type": "integer",
      "minimum": 1,
      "maximum": 3600000
    },
    "environmentName": {
      "type": "string",
      "pattern": "^[A-Za-z_][A-Za-z0-9_]*$"
    }
  }
}
